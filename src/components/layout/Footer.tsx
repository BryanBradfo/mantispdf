export default function Footer() {
  return (
    <footer className="border-t border-gray-200 bg-white py-6 text-center text-sm text-gray-500">
      <p>MantisPDF — All processing happens in your browser. No data is uploaded.</p>
    </footer>
  );
}
